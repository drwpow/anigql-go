type FrameSequence {
  ID: String!
  # artists: [PersonEdge]!
  # film: Film!
  # frames: [Frame]!
  # keyframeArtists: [PersonEdge]!
  notes: String
  order: Int!
}

type Frame {
  # artists: [PersonEdge]
  image: String!
  keyframe: Boolean!
  notes: String
  order: Int
}
